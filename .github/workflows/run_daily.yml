name: Sijishe Daily Check-in

on:
  schedule:
    # 多时间点 + 非整点(避开 GitHub 整点拥堵)，最大化触发成功率
    # 脚本本身幂等：首次签到成功后，后续运行识别"今日已签"直接跳过，不会重复签
    - cron: '17 1 * * *'    # 北京时间 09:17
    - cron: '17 4 * * *'    # 北京时间 12:17
    - cron: '17 7 * * *'    # 北京时间 15:17
    - cron: '17 10 * * *'   # 北京时间 18:17
    - cron: '17 13 * * *'   # 北京时间 21:17
  workflow_dispatch:        # 允许手动运行

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # 回写 Secret 需要 actions 写权限；contents: read 用于 checkout
    permissions:
      actions: write
      contents: read

    steps:
      # ===================== 拉代码 ===================== #
      - name: Checkout code
        uses: actions/checkout@v4

      # ===================== 装 Python ===================== #
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # ========== 安装 Cloudflare WARP ========== #
      - name: Install Cloudflare WARP
        run: |
          set -e
          curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | \
            sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
          echo "deb [arch=amd64 signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] \
            https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | \
            sudo tee /etc/apt/sources.list.d/cloudflare-client.list
          sudo apt-get update
          sudo apt-get install -y cloudflare-warp

      # ========== 启动 WARP SOCKS5 代理 ========== #
      - name: Start WARP SOCKS5 proxy
        run: |
          set -e
          warp-cli --accept-tos registration new
          warp-cli --accept-tos mode proxy
          warp-cli --accept-tos proxy port 40000
          warp-cli --accept-tos connect
          sleep 5
          echo "--- WARP 状态 ---"
          warp-cli --accept-tos status || true
          echo "--- 验证出口 IP（应显示 Cloudflare IP）---"
          curl -s --socks5 127.0.0.1:40000 https://ifconfig.me; echo

      # ===================== 装依赖 ===================== #
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r src/requirements.txt

      # ========== 通过 WARP 代理跑签到 ========== #
      - name: Run check-in via WARP
        env:
          SIJISHE_USERNAME: ${{ secrets.SIJISHE_USERNAME }}
          SIJISHE_PASSWORD: ${{ secrets.SIJISHE_PASSWORD }}
          SIJISHE_COOKIE: ${{ secrets.SIJISHE_COOKIE }}
          # 账号密码登录成功后，把新鲜 Cookie 导出到该文件，供下一步回写 Secret
          COOKIE_EXPORT_FILE: ${{ github.workspace }}/src/fresh_cookie.txt
          ALL_PROXY: socks5://127.0.0.1:40000
        run: |
          cd src
          python sijishe.py

      # ========== 把本次登录得到的新鲜 Cookie 回写 GitHub Secret ========== #
      - name: Persist fresh cookie to Secret
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          FILE="${{ github.workspace }}/src/fresh_cookie.txt"
          if [ -f "$FILE" ]; then
            echo "📥 发现新鲜 Cookie，回写 SIJISHE_COOKIE Secret..."
            gh secret set SIJISHE_COOKIE < "$FILE"
            echo "✅ Secret SIJISHE_COOKIE 已更新（下次优先用新 Cookie，无需再降级）"
            rm -f "$FILE"
          else
            echo "ℹ️ 本次未走账号密码登录（Cookie 仍有效），无需更新 Secret"
          fi

      # ========== 关闭 WARP（清理） ========== #
      - name: Stop WARP
        if: always()
        run: |
          warp-cli --accept-tos disconnect || true
          echo "✅ WARP 已断开"
